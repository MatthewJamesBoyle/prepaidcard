server:
  port: 8080
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
zuul:
  routes:
    account: /account/**
    transaction: /transaction/**
    merchant: /merchant/**